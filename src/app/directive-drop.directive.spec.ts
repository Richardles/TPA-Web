import { DirectiveDropDirective } from './directive-drop.directive';

describe('DirectiveDropDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveDropDirective();
    expect(directive).toBeTruthy();
  });
});
